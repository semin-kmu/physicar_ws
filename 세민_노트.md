# 타 패키지 수정 요청 — kau_state_machine 연동

`kau_state_machine` 구현 중 발견한 **타 패키지 수정 사항**.
근거는 `src/kau_state_machine/docs/` 계약 번호로 표기.

---

## A. 즉시 필요 — 순차 기동(MVP) 차단

### A-1. `kau_localization` · `platform_ekf_pause.py` 를 실행파일로 설치

**현재**: `share/kau_localization/launch/` 에만 설치됨 (`install(DIRECTORY launch ...)`).
`ekf.launch.py` 가 `sys.executable` 로 직접 호출.

**문제**: `system_supervisor` 는 전 노드를 `lib/<pkg>/<exe>` 규칙으로 spawn.
이 파일만 경로 규칙이 달라 예외 처리가 생김.

**요청**: `kau_localization/CMakeLists.txt` 에 1줄 추가

```cmake
install(PROGRAMS launch/platform_ekf_pause.py DESTINATION lib/${PROJECT_NAME})
```

- 기존 `install(DIRECTORY launch ...)` 는 그대로 둘 것 (`ekf.launch.py` 가 계속 참조)
- 파일 수정 불필요. shebang·argv 인터페이스 그대로 사용

---

## B. 감시·복구 단계 착수 전 필요

MVP(순차 spawn) 에는 불필요. `02` 감시 / `03` 복구 구현 시 차단됨.

### B-1. 전 노드 · `/diag/heartbeat` 발행 (`02` 계약 19, `01` 계약 7)

**현재**: 발행 노드 **0개**.

**요청**: 분류 A 7개 노드가 `/diag/heartbeat` 를 **10 Hz 고정**, **주 tick 안에서** 발행

| 항목 | 값 |
| --- | --- |
| 타입 | `kau_msgs/Heartbeat` (→ C-1 신설 필요) |
| QoS | BEST_EFFORT · VOLATILE · depth 5 |
| `node_id` | `bringup.yaml` 노드명과 **완전 일치** (`02` 계약 20) |
| 필수 필드 | `status` · `seq` · `loop_period_max_ms` · `inputs[]` |

- 별도 타이머 금지. 주 tick 안에서 발행해야 루프 정지가 검출됨
- 이게 없으면 Supervisor 가 노드 이상을 **감지할 수단이 없음**

### B-2. 전 노드 · Lifecycle Node 전환 (`01` 계약 1~4, 11)

**현재**: Lifecycle 사용 노드 **0개**. 전부 평범한 `rclcpp::Node`.

**요청**: 분류 A 7개 노드를 `rclcpp_lifecycle::LifecycleNode` 로 전환

| # | 요구 |
| --- | --- |
| 1 | 무거운 초기화 전부 `on_configure` (카메라 open, 모델 로드, 파일 읽기) |
| 2 | `INACTIVE` 에서 발행 금지 |
| 3 | `on_activate` 는 publisher 활성화만 |
| 4 | `on_cleanup` 에서 상태 완전 초기화 |

- 없으면 관문 G3 불가 → 준비 판정이 토픽 관측 단독이 됨
- 없으면 Soft Reset(`03`) 불가 → 모든 복구가 **프로세스 재시작**으로 격상

### B-3. `kau_control` · 정지 게이트 2계층 구독 (`01` 계약 12~14, 17)

**현재**: 두 컨트롤러 모두 `/state_machine/enable` · `/state_machine/speed_limit` 를
**구독하지 않음**. Deadman 미구현 (`README.md:44` — "deadman 은 kau_state_machine 소관").

**요청**:

| # | 요구 | 대상 |
| --- | --- | --- |
| 12 | Deadman 공용 구현 1벌. `deadman_timeout_ms` **두 노드 동일값 (250)** | speed, steer |
| 13 | 속도 상한을 **EMA 이후**에 `min` 으로 적용. 대기 중에도 내부 연산 상시 수행 | speed |
| 14 | 게이트 입력은 `enable` · `speed_limit` **2개만**. `start_permission` 직접 구독 금지 | speed |
| 17 | `speed_limit` 에도 stale timeout (250 ms). 미수신 시 `limit = 0` | speed |

- `enable` 끊김 → 즉시 안전값. `speed_limit == 0` → 즉시 정지
- **정적 속도 상한 파라미터를 두지 말 것**. 상한 원천은 `/state_machine/speed_limit` 하나 (`01` section 2-2)
- 현재 `speed.v_max: 2.0` 이 `speed_controller.yaml` 에 있음 → 원천 이중화. 처리 방침 협의 필요

### B-4. 전 노드 · `header.stamp` 필수 (`01` 계약 6)

stale 판정(`02`)의 유일 근거. 전 출력 메시지에 채울 것.

### B-5. 전 노드 · 출력 토픽 주기 발행 (`01` 계약 5)

검출 0개여도 **빈 메시지 발행**. 미발행과 고장을 구분할 수 없음.

---

## C. `kau_msgs` · 메시지 신설

**현재 보유**: `KauPath` · `ObstacleCircle` · `ObstacleCircleArray` 3종.

**필요**: `07` section 4 정의 기준 8종 (전부 미존재)

| 메시지 | 발행 | 용도 |
| --- | --- | --- |
| `Enable` | system_supervisor | Deadman 게이트 |
| `SpeedLimit` | state_machine | 속도 상한 + 사유 enum |
| `SystemState` | system_supervisor | System 7상태 |
| `MissionState` | state_machine | Mission 상태 + `s_mono` |
| `BehaviorState` | state_machine | Behavior 4상태 |
| `PlannerProfile` | system_supervisor | 축소 주행 프로파일 |
| `Heartbeat` | 분류 A 7노드 | B-1 |
| `SupervisorHealth` | system_supervisor | GUI 전용 |

- 정식 필드 정의는 `docs/07_인터페이스.md` section 4
- **`kau_state_machine` 이 작성 후 PR 예정.** 타 파트는 수용 여부만 확인

---

## D. 불일치 — 확인 필요

문서 가정값과 실제 구현이 다름. **어느 쪽에 맞출지 결정 필요.**

### D-1. 토픽명

| 문서 (`07`) | 실제 구현 | 위치 |
| --- | --- | --- |
| `/steer` | **`/steering`** | `kau_control/src/steer_controller_node.cpp:56` |
| `/perception/objects` | **`/perception/obstacles`** | `kau_object_detection/src/laser_scan_clusterer_node.cpp:127` |

- `/steer` 는 `07` 에서 "차량측 확정, 우리가 정하지 않음" → **차량 인터페이스 확인 필요**
- `/perception/objects` 는 `07` 에서 "미정 · 가정값" → **실제값 `/perception/obstacles` 로 문서 수정 예정**

### D-2. 메시지 타입

| 토픽 | 문서 가정 | 실제 |
| --- | --- | --- |
| `/perception/obstacles` | `kau_msgs/DetectedObjectArray` | `kau_msgs/ObstacleCircleArray` |
| `/speed` · `/steering` | `kau_msgs/TargetSpeed` / `TargetSteer` | `std_msgs/Float64` |

`/speed` · `/steering` 타입은 **차량 인터페이스 확정 대기** (`07` section 2-3).

### D-3. `use_sim_time` 기본값이 `true`

| 파일 | 줄 |
| --- | --- |
| `kau_localization/launch/amcl.launch.py` | 193 |
| `kau_localization/launch/cartographer_localization.launch.py` | 157 |
| `kau_localization/launch/ekf.launch.py` | 165 |
| `kau_localization/launch/slam.launch.py` | 89 |
| `kau_control/launch/control.launch.py` | 50 |
| `kau_lane_detection/launch/lane_detection.launch.py` | 102 |

실차 기동 시 전부 `false` 필요. `bringup.yaml` 에서 일괄 주입할 예정이므로 **launch 기본값 변경은 불필요**.
단, 개별 launch 로 테스트할 때는 매번 `use_sim_time:=false` 를 붙여야 함.

### D-4. 노드 동작 주기 미측정 (`08` section 4-0)

**전 노드의 실제 발행 주기가 미확정.** stale 임계 · G4 주기 임계가 전부 여기 종속.

**요청**: 각 파트가 출력 토픽의 **실측 주기** 회신

| 노드 | 토픽 | 실측 주기 |
| --- | --- | --- |
| `laser_scan_clusterer` | `/perception/obstacles` | ? |
| `kau_lane_detection_node` | `/lane/center` | ? |
| `local_planner_node` | `/path/local` | ? (`07` 은 5 Hz 확정 표기) |
| `kau_ekf` | TF `odom→base_footprint` | ? |
| `amcl` | TF `map→odom` | ? |
| `speed_controller` | `/speed` | ? (`control_hz` 기본 50) |
| `steer_controller` | `/steering` | ? |

산출 규칙: G4 임계 = 주기 × 0.8 / stale 임계 = 주기 × 3

### D-5. 도커 이미지가 `RMW_IMPLEMENTATION` 을 강제로 덮어씀

**위치**: `kau-amet:jazzy` 이미지의 `/etc/bashrc.d/10-ros.sh:15`

```sh
export RMW_IMPLEMENTATION=rmw_zenoh_cpp
```

**문제**: 무조건 `export` 라 `docker run -e RMW_IMPLEMENTATION=...` 가 무시된다
(로그인·대화형 셸 양쪽에서 로드됨).

**설계 문서와 충돌**: `README.md` · `00_치트시트.md` 는
**`rmw_fastrtps_cpp` 확정 / `rmw_zenoh` 사용 불가 (대회 차량에 미설치)**.

**요청**: 기본값만 주고 외부 지정을 존중하도록 변경

```sh
export RMW_IMPLEMENTATION="${RMW_IMPLEMENTATION:-rmw_zenoh_cpp}"
```

- `01` section 10 사전 점검이 `RMW_IMPLEMENTATION == rmw_fastrtps_cpp` 를 확인하도록 규정됨
- 현재는 컨테이너 안에서 이 검사를 통과시킬 방법이 없음

### D-6. `rclpy` 노드가 SIGTERM 에서 traceback 을 남김 (`01` 계약 10)

**증상**: supervisor 가 `SIGTERM` 을 보내면 `rclpy.spin()` 이
`ExternalShutdownException` 을 던지는데, `KeyboardInterrupt` 만 잡는 코드는
traceback 을 찍고 **exit code 1** 로 죽는다.

**대상**: `kau_global_path/scripts/*.py` 등 rclpy 노드 전부
(`kau_state_machine` 2개 노드는 수정 완료)

**요청**:

```python
from rclpy.executors import ExternalShutdownException
...
except (KeyboardInterrupt, ExternalShutdownException):
    pass
```

- C++ 노드는 해당 없음 (`rclcpp` 가 자체 처리)
- 계약 10 "SIGTERM 수신 시 3 s 내 정리 종료" 충족을 위해 필요

### D-7. `kau-amet:jazzy` 이미지에 localization 의존 패키지가 없음

**증상**: `colcon build` 시 `kau_localization` 이 `cartographer_ros_msgs` 를 못 찾아 실패.
연쇄로 나머지 5개 패키지가 `Aborted`.

**확인 결과** — 이미지에 전부 미설치

| 패키지 | 필요한 곳 |
| --- | --- |
| `cartographer_ros` · `cartographer_ros_msgs` | `kau_localization` 빌드 |
| `robot_localization` | `bringup.yaml` 단계 2 (`kau_ekf`) |
| `nav2_map_server` · `nav2_amcl` · `nav2_lifecycle_manager` | `bringup.yaml` 단계 3 |

**영향**: 이 컨테이너에서는 **기동 단계 2~3 을 검증할 수 없음**.
`system_supervisor` 는 단계 2 에서 `실행 불가` 로 중단된다.

**확인 필요**: 대회 차량에는 이 패키지들이 설치되어 있는지.
차량 이미지가 다르다면 검증 환경을 그쪽에 맞춰야 함.

---

## E. 참고 — 수정 불필요

조사 중 확인했으나 **문제 없는 항목**.

| 항목 | 상태 |
| --- | --- |
| `/perception/start_permission` | `std_msgs/Bool`, 기본 출력 토픽 일치. `01` 계약 15 충족 |
| 파라미터 yaml 최상위 키 | 전부 노드명과 일치 → `--params-file` 직접 주입 가능 |
| `map_amcl_lifecycle_manager` | `autostart: true`, `node_names: [map_server, amcl]` → AMCL lifecycle 전이 자동 처리 |
| `/path/global` · `/path/local` · `/lane/center` | 문서와 실제 일치 |

---

## 우선순위 요약

| 순위 | 항목 | 차단 대상 |
| --- | --- | --- |
| 1 | A-1 | 순차 기동 MVP |
| 2 | D-7 (이미지 의존성) | 단계 2~3 검증 불가 |
| 2 | D-5 (RMW 강제) | 컨테이너에서 사전 점검 불가 |
| 3 | D-1 · D-2 · D-4 회신 | `bringup.yaml` 확정 |
| 4 | D-6 (rclpy SIGTERM) | 종료 시 노드가 비정상 코드로 죽음 |
| 5 | C (메시지 8종) | `enable` · `speed_limit` 발행 |
| 6 | B-3 (컨트롤러 게이트) | **차량 정지 수단 전체** |
| 7 | B-1 (heartbeat) | 감시 (`02`) |
| 8 | B-2 (Lifecycle) | 복구 (`03`) |
