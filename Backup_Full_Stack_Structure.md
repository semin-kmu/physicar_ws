### 백업용 풀스택 구현

#### 필요 패키지
- Backup Lane Detection
- Backup Object Detection
- Backup Speed Controller
- Backup Steer Controller
- Backup Path Planner

<br>

---

#### 각 패키지 별 역할
##### Backup Lane Detection

100도 단안 카메라를 사용해 전방의 차선을 검출
기존의 kau_lane_detection 패키지를 활용하되, 성능 개선
(다만, 현재의 차선 검출 성능이 좋지 못하므로 더 나은 방법을 적극적으로 탐색해봐야함)

현재 카메라 Hz (/camera/image_raw/compressed 기준) 11~12Hz
크게 카메라 이미지 토픽은 /camera/image_raw와 /camera/image_raw/compressed 2개가 publish되고있는데, 후자가 데이터 양이 적어 실시간성에 유리할 것 (확인은 필요함)

따라서, /camera/image_raw/compressed 토픽을 입력받아 **흰색 차선 만** 검출
이때 카메라 해상도를 몇으로 설정하는 것이 좋을 지는 확인 필요

intrinsic도 기존의 kau_lane_detection 패키지에서 사용한 intrinsic을 그대로 활용

다만, kau_lane_detection 패키지에서 사용한 BEV 변환에 지연이 존재하거나 연산 부하가 무시하기 어려운 수준이라면, BEV 변환은 하지 않는 것도 고려중

자, 그러면 카메라로 검출한 흰색 차선을 어떻게 사용할거냐...

크게 3가지 정보를 추출해야함.
1. 두 흰색 차선을 각각 직선으로 표현
2. 두 흰색 차선의 중앙을 하나의 직선으로 표현
3. 이때 곡선 구간의 경우 본 대회 트랙은 R값이 매우 작기 때문에, 연산량 최적화를 위해 두 직선으로 표현

1번에 대해 자세하게 기술하면...
카메라에서 검출된 흰색 차선 중에 가장 낮은 픽셀에 있는 곳을 시작점으로 사용, 특정 신뢰도를 만족하는 조건 하에 가장 높이 있는 픽셀에 있는 곳을 종점으로 사용

시작점과 종점을 단순 직선으로 보간

3번에 대해 자세하게 기술하면...
1번에서 검출하는 과정에서 
물론 약한 곡률의 경우 직선으로 근사하겠지만, 20도 이상의 각도가 검출된 경우 곡선으로 간주

이때 **변곡점** 이전과 이후를 별도의 곡선으로 처리 
\* 변곡점은 추후 Speed Controller의 감속 기준에 사용될 예정

변곡점 이후의 직선에 대해서는 변곡점 이전의 직선과의 각도 정보를 검출해야함 (코너가 몇도인지 알기 위함)

2번에 대해 자세하게 기술하면...
단순 직선의 경우 차선 중앙으로 차량이 이동할 필요성을 무시할 수 없음
따라서 곡선 진입/탈출 이후 차선 중앙으로 차량이 정렬되도록 차선의 중앙을 하나의 직선으로 표현해 차량이 추종해야 할 기준점으로 사용

다만, 분명 주행 중 진동이나 외부 요인 등으로 인해 해당 중앙 직선이 진동(Jitter)할 것임은 자명하며, 이를 완화 및 극복하기 위해 방법을 모색해야함.

아, 그리고 camera pan은 비활성화 할 것임
불필요한 변수라고 생각중이며, camera pan 없이는 해결 불가능한 문제 발생 시 도입을 고려해 볼 것임

<br>

---

##### Backup Object Detection
기존의 kau_object_detection 코드를 거의 그대로 사용
차량 본인에 대한 근처 장애물의 상대 좌표 및 크기를 검출 및 출력

차량이 주행 간에 장애물을 회피하기 위한 정보를 제공

<br>

---

##### Backup Speed Controller
먼저, Max Target Speed는 조향 액추에이터 성능 (최대 각속도 600deg/s, 응답 시정수 모름)과 차량 스펙 (휠베이스 0.18m)에 따라 동역학적/수학적으로 상한이 존재
해당 Max Target Speed를 이론적으로 확립시켜놓은 후, 곡선 구간에서는 필요한 만큼만 감속하며 진행하는 것을 목표함

Speed Controller는 50Hz로 동작

또한, Backup Lane Detection 에서 계산해내는 차선 검출 최대 길이 및 동작 Hz(11~12Hz)에 따라 Max Target Speed가 하양조정될 수 있음

현재까지는 Min Target Speed를 0.5 ~ 1.0m/s 정도로 고려중
가속도와 감속도를 정의할 것인데, 원하는 가속도는 1.5m/s^2, 0.5m/s^2
여기에 가속과 감속은 1초에 완료되기를 원하며, 이 조건들을 바탕으로 동적 Target Speed Adjust 설계가 알아서 역산될 것이라 생각중 (더 필요한 정보가 있으면 편하게 물어볼 것)

**안전을 위해 노드가 종료될 시 /speed 토픽 publish를 멈추거나, 강제로 0으로 publish하는 등의 로직 필요**

<br>

---

##### Backup Steer Controller
먼저, 트랙의 최대 폭은 70.8cm
본 설계는 map -> odom -> base_link TF 관계가 없는, 즉 절대 좌표 시스템이 존재하지 않는 상대 좌표 기반의 경로 추종 패키지가 될 것임.
직진 경로의 경우 Backup Lane Detection 에서 계산한 중앙 직선 Path를 추종할 것이며, 곡선 경로의 경우 Backup Lane Detection 에서 계산한 변곡점 이전/이후 직선 간의 각도만을 사용해 명령 조향각을 계산할 것임.
이는 빠른 주행 속도에서 좁은 차선을 카메라가 좌우 전부 다 인식하는 것은 한계가 있다 판단해 구상한 방안이며, 좌측 또는 우측 중 1개 차선이 검출되지 않을 때 조향하기 시작해 다시 2개 차선이 검출되면 바로 다음 나오는 직선을 자연스럽게 추종하도록 될 것임

이때 목표 조향각은 0도 <-> 변곡점 각도 로 변하게 될 것이며, 실제 명령 조향각은 PID를 사용해 이산적이지 않고 부드럽게 각도 오차를 0으로 만들게 될 것임

따라서 Backup Lane Detection, Backup Speed Controller, Backup Steer Controller 이 3개 패키지는 서로 연관성이 짙음. Max Target Speed에 따라 1초 안에 가감속이 이루어지기 위한 변곡점 검출 거리가 정해질 것이며, 차선 폭에 따라 변곡점 각도를 목표 조향 각도로 반영시키기 위한 변곡점 검출 거리가 이어서 정해질 것임

<br>

---

##### Backup Object Detection
기존의 kau_object_detection 패키지에서 사용중인 신호등 검출 및 publish 로직과 장애물 검출 로직을 그대로 사용

빠른 속도로 주행하는 것이 목표이기 때문에, 후에 추가적인 전처리 또는 후처리가 필요할 수 있음

장애물 검출은 오직 2D LiDAR만 사용, 따라서 동작 주기는 현재 사용중인 /scan 의 Hz와 맞추는게 좋다고 판단됨

<br>

---

##### Backup Path Planner

해당 패키지의 목적은 차선 안에서 경로 추종하고있는 차량이 장애물을 만났을 때 장애물을 회피하도록 하기 위함임
먼저 대회 규정 상 양 차선을 차량이 이탈하면 절대 안됨
따라서 차선 내에서만 회피가 이루어져야하며, 이러한 제약을 설정하기 위해 별도의 Path Planner를 둠

기본적으로는 Backup Lane Detection 패키지에서 계산하는 중앙 경로를 추종하고, 특정 거리 이내에 장애물이 있으면 offset curve 방식 또는 더 나은 방법으로 회피경로를 생성한다


<br>

---




멀리서 보이면 미리 감속 시작, 임계값 도달 시 회전 시작